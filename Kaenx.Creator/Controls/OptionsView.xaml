<UserControl
    x:Class="Kaenx.Creator.Controls.OptionsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:p="clr-namespace:Kaenx.Creator.Properties"
    xmlns:System="clr-namespace:System;assembly=mscorlib"
    xmlns:conv="clr-namespace:Kaenx.Creator.Converter"
    xmlns:models="clr-namespace:Kaenx.Creator.Models;assembly=Kaenx.Creator.Share"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    x:Name="ViewControl">
    <UserControl.Resources>
        <conv:EmptyToBool x:Key="EmptyToBool" />
        <conv:EnumToVisibility x:Key="EnumToVis" />
        <conv:BoolToVisibility x:Key="BoolToVis" />
        <conv:BoolNegation x:Key="BoolNeg" />
        <conv:BoolToBrush x:Key="BoolToBrush" TrueValue="Red" FalseValue="Black" />
        <conv:StateToBrush x:Key="StateToBrush" />
    </UserControl.Resources>

    <StackPanel>
        <StackPanel.Resources>
            <Style TargetType="HeaderedContentControl">
                <Setter Property="Margin" Value="5" />
                <Setter Property="IsTabStop" Value="false" />
            </Style>
            <Style TargetType="CheckBox">
                <Setter Property="Margin" Value="5" />
            </Style>
            <Style TargetType="ComboBox">
                <Setter Property="Margin" Value="0 5" />
            </Style>
            <Style TargetType="Button">
                <Setter Property="Margin" Value="2.5" />
            </Style>
            <Style TargetType="ListView">
                <Setter Property="Margin" Value="5" />
            </Style>
        </StackPanel.Resources>

        <CheckBox Content="Supports Extended Memory Services" IsChecked="{Binding Path=Version.Options.SupportsExtendedMemoryServices, ElementName=ViewControl}" />
        <CheckBox Content="Supports Extended Property Services" IsChecked="{Binding Path=Version.Options.SupportsExtendedPropertyServices, ElementName=ViewControl}" />
        <CheckBox Content="Supports IPSystemBroadcast" IsChecked="{Binding Path=Version.Options.SupportsIPSystemBroadcast, ElementName=ViewControl}" />
        <CheckBox Content="Prompt Before Full Download" IsChecked="{Binding Path=Version.Options.PromptBeforeFullDownload, ElementName=ViewControl}" />
    </StackPanel>

</UserControl>